# GitHub OAuth App Client ID for admin sign-in.
# Create an OAuth App at https://github.com/settings/developers
# and set Authorization callback URL to: https://your-site.com/login/callback
# (or http://localhost:5173/login/callback for local dev)
VITE_GITHUB_CLIENT_ID=your_github_oauth_client_id
